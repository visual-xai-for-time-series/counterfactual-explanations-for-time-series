# Core dependencies for cfts package
numpy == 2.0.1
scipy == 1.14.1
scikit-learn == 1.5.1
torch == 2.3.1
torchvision == 0.18.1
captum == 0.7.0
tqdm == 4.66.5

# For DTW distance metric in proximity metrics
dtaidistance == 2.3.12

# For DisCOX discord-based counterfactual generation
stumpy >= 1.12.0

# For MG-CF and Time-CF shapelet-based methods
pyts >= 0.12.0
tslearn >= 0.6.3
fastdtw >= 0.3.4

# For UMAP-based projection plots in the IMFACT ablation
umap-learn >= 0.5.6

# For EMD-based IMF decomposition in IMFACT (optional decomposer="emd").
# This is the "emd" package (emd.sift.sift) — NOT the unrelated
# "EMD-signal"/PyEMD package, which has a different, incompatible API.
emd >= 0.6.0

# For the wavelet decomposition path in FFT-CF (cfts/cf_fft_cf/fft_cf.py)
PyWavelets >= 1.8.0

# For MASCOTS' BoRF pipeline (cfts/cf_mascots/_fast_borf.py); required to
# run mascots_cf, not just to import cfts.
sparse == 0.19.2
numba == 0.61.2